{
  "name": "planner-tests",
  "private": true,
  "description": "Constraint and behaviour checks for the lesson planner.",
  "scripts": {
    "audit": "node audit.js",
    "test": "node run-all.js"
  },
  "devDependencies": { "jsdom": "^24.0.0" }
}
